# MkDocs — documentation site only (GitHub Pages)
# Install: pip install -r requirements-mkdocs.txt
# Build:   mkdocs build
# Deploy:  mkdocs gh-deploy

mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
pymdown-extensions>=10.8
